<h1>My Blog</h1>
<div>
    <% @blog_posts.each do |blog_post| %>
    <%# <h2><%= link_to blog_post.title, blog_post_path(blog_post) %>
    <%# Because Rails will infer, you can just pass the blog_post to the link_to instead of the full blog_post_path with arg %>
    <h2><%= link_to blog_post.title, blog_post %></h2>
    <%# Do not need to explicitly pass blog_post.id as rails will infer from the item passed %>
    <p><%= blog_post.body %></h2>
    <% end %>
</div>